# PCG-ready city dressing

The generator emits deterministic, spatially partitioned placement candidates for lamps, bollards, benches, bins and bicycle racks from real GIS features. It does not pretend these are surveyed individual objects.

## Remaining
1. bind each role to audited production meshes;
2. create UE 5.8 PCG Graph/Data assets and ingest the placement catalogue;
3. assign Data Layers/HLOD layers per role;
4. add collision/nav exclusion and street-side orientation;
5. extend rules with verified OSM/BDOT10k street furniture where present;
6. measure density/draw calls and run screenshot QA before promotion.
